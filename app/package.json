{
  "name": "polymatch3-app",
  "version": "1.0.0",
  "private": true,
  "description": "PolyMatch3 Capacitor 离线壳",
  "scripts": {
    "sync": "cap sync",
    "build": "cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  }
}
